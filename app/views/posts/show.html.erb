<!-- Container for displaying content -->
<div class="container">
  <!-- Flash messages -->
  <% flash.each do |key, value| %>
    <div class="flash <%= key %>">
      <%= value %>
    </div>
  <% end %>

  <!-- Display details of the post -->
  <div class="post">
    <!-- Post title -->
    <h4>Post <%= @post.title %></h4>
    <!-- Count of comments and likes for the post -->
    <p class="count">Comments: 💬<%= @post.comments.count %> | Likes: 📝<%= @post.likes.count %></p>
    <!-- Post content -->
    <p><%= @post.text %></p>
  </div>

  <!-- Display comments for the post -->
  <div class="comments">
    <% @post.comments.each do |comment| %>
      <!-- Individual comment section -->
      <div class="comment">
        <!-- Comment author and text -->
        <p>👤<%= comment.user.name %>: <%= comment.text %></p>
      </div>
    <% end %>
  </div>

  <!-- Link to create a new comment -->
  <%= link_to 'Create New Comment 💬', new_user_post_comment_path(@user, @post), class: 'btn-comment' %>

  <!-- Link to like the post -->
  <%= link_to 'Like 👍', new_user_post_like_path(@user, @post), class: 'btn-like' %>
</div>
